mutation RegisterDelivery($options: EmailPasswordInput!, $avatar: Upload!) {
  registerDelivery(options: $options, avatar: $avatar) {
    delivery {
      email
    }
    errors {
      field
      message
    }
  }
}